pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
    // Assurez-vous que les plugins Android ne sont pas définis ici
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "The Rift"
include(":app")
